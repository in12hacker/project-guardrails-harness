{
  "schema_version": "3.0",
  "change_class": "control_logic",
  "compatible": true,
  "affected_control_ids": [
    "*"
  ],
  "summary": "Hardens every v3 control's portable evidence persistence boundary with canonical diagnostic text, create-only atomic publication, deterministic shared-read POSIX modes, symlink containment, and typed unreadable-evidence failure; active task lint now reads the manifest campaign directly so a saved candidate cannot become a second revision authority; no compatibility reader or schema change is introduced."
}

{
  "schema_version": "3.0",
  "change_class": "control_logic",
  "compatible": true,
  "affected_control_ids": [
    "*"
  ],
  "summary": "Hardens the portable evidence persistence boundary for every v3 control: canonical redacted diagnostic text prevents whitespace pollution, new content-addressed objects are atomically published with deterministic shared-read POSIX modes, symlink escape is rejected, and unreadable evidence becomes a deterministic validation error; existing evidence remains immutable and no compatibility reader or schema change is introduced."
}
